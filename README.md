# deepseek-finetune-word-reversal
Fine-tuning DeepSeek-R1 to reverse words.

## Instructions

### Data

1. Define a new `data/` folder, where you should give a .txt file such as `words.txt`. This file contains words used to train the model.
2. Arguments of `generate_data.py` can be used to modify the dataset generation for the task.
